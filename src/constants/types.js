// http://json-schema.org/latest/json-schema-core.html#anchor8
const ARRAY = 'array';
const BOOLEAN = 'boolean';
const INTEGER = 'integer';
const NUMBER = 'number';
const NULL = 'null';
const OBJECT = 'object';
const STRING = 'string';

export default {
  ARRAY,
  BOOLEAN,
  INTEGER,
  NUMBER,
  NULL,
  OBJECT,
  STRING,
};
